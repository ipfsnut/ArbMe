[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
dependsOn = []
cmds = [
  "pnpm --filter @arbme/core-lib build",
  "pnpm --filter @arbme/nextjs build"
]

[start]
cmd = "cd bot && pnpm server"
