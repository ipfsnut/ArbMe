{
  "name": "arbme-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter nextjs dev",
    "build": "pnpm -r build",
    "bot": "pnpm --filter bot dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
